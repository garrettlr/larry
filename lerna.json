{
  "packages": [
    "packages/libs/*",
    "packages/web/*",
    "packages/mobile/*",
    "packages/services/*"
  ],
  "version": "independent",
  
  "command": {
    "version": {
      "allowBranch": ["master"],
      "conventionalCommits": true,
      "message": "ci(release): releasing artifacts"
    }
  }
}
