{
	"name": "{{projectName}}",
	"description": "{{projectDescription}}",
	"version": "{{projectVersion}}",
	"private": true,
	"publishConfig": {},
	"bin": {},
	"files": [],
	"repository": {
		"type": "git",
		"url": "https://github.com/{{githubProjectUser}}/{{githubProjectName}}"
	},
	"license": "MIT",
	"scripts": {
		"clean-slate": "echo 'Remove all the node_modules' && lerna clean  --yes && rm -rf node_modules",
		"setup-project": "npm install && lerna bootstrap --hoist --force-local"
	},
	"dependencies": {},
	"devDependencies": {
		"lerna": "^3.20.2"
	}
}